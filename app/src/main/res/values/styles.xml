<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:navigationBarColor">#229999</item>
        <item name="actionOverflowMenuStyle">@style/OverflowMenuStyle</item>
        <item name="android:colorControlNormal">#ffffff</item>
        <item name="android:actionOverflowButtonStyle">@style/OverflowMenuIcon</item><!--自定义菜单图标-->
    </style>


    <style name="ToolBarPopupTheme" parent="@style/ThemeOverlay.AppCompat">
        <!-- 是否覆盖锚点，默认为true，即盖住Toolbar -->
        <item name="android:overlapAnchor">false</item>
        <item name="android:dropDownWidth">wrap_content</item>
        <item name="android:paddingRight">5dp</item>
        <!-- 弹出层背景颜色 -->
        <!-- 弹出层垂直方向上的偏移，即在竖直方向上距离Toolbar的距离，值为负则会盖住Toolbar -->
        <item name="android:dropDownVerticalOffset">5dp</item>
        <!-- 弹出层水平方向上的偏移，即距离屏幕左边的距离，负值会导致右边出现空隙 -->
        <item name="android:dropDownHorizontalOffset">0dp</item>
        <item name="android:colorBackground">#FF9898</item><!--弹出菜单的背景颜色-->
        <item name="android:textColor">#ff2154</item><!--弹出菜单的文字颜色-->
    </style>

    <style name="OverflowMenuStyle" parent="Base.Widget.AppCompat.PopupMenu.Overflow">
        <item name="overlapAnchor">false</item><!--设置不覆盖锚点-->
        <item name="android:dropDownWidth">wrap_content</item>
    </style>

    <style name="OverflowMenuIcon" parent="Base.Widget.AppCompat.ActionButton.Overflow">
        <item name="android:src">@drawable/pineapple_pic</item>
    </style>
</resources>
